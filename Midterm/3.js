let age = 0.0;

function checkAge() {
    if (age.toString() !== "1.4") {
        return;
    } else {
        return 1;
    }
}

/*
There is no float type in JavaScript

There is no private keyword in JavaScript, and function names cannot have Spaces.

The non-equal operator in JavaScript is !==

keyword in JavaScript is else
*/